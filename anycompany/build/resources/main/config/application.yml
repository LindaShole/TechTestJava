server:
  port: 8887

me:
  app:
    api-prefix: '/api'

spring:
  datasource:
    url: jdbc:h2:file:orderdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: false
