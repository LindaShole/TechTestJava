spring:
  application:
    name: tech-test-java

  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb
    username: ${DB_USER}
    password: ${DB_PWD}

  liquibase:
    enabled: false